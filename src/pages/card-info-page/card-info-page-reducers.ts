import { start } from "repl";

const INIT_STATE = {

};

export const cardInfoPageReducer = (state = INIT_STATE, { action, payload }) => {
    switch(action) {
        default: {
            return state;
        }
    }
};